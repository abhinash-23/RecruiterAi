/**
 * Super Admin's backend surface — the platform-level endpoints only this role
 * can reach. Import from here rather than from the individual files.
 *
 *   import { usePlatformAdmins, type PlatformAdmin } from "@/services/super-admin"
 */

export * from "./platform-admins"
export * from "./use-platform-admins"
export * from "./platform"
export * from "./use-platform"

export * from "./settings"
